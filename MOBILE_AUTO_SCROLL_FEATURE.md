# 모바일 대시보드 자동 스크롤 기능

## 🎯 기능 개요

모바일 태스크 대시보드에서 **현재 시간 기준 다음에 수행할 태스크**로 자동 스크롤하는 기능이 추가되었습니다.

## ✨ 주요 기능

### 1. 자동 스크롤
- **페이지 로드 시**: 자동으로 다음 태스크로 스크롤
- **사이트/날짜 변경 시**: 자동으로 다음 태스크로 스크롤
- **Auto Refresh 시**: 5분마다 새로고침 후 자동 스크롤

### 2. 다음 태스크 감지 로직

```javascript
다음 태스크 = 
  현재 시간 이후 AND
  미완료 상태 AND
  가장 가까운 시간
```

### 3. 시각적 표시

**다음 태스크 (NEXT):**
- 🔴 빨간색 배경 (깜빡임)
- 🔴 빨간색 테두리
- ▶ NEXT 라벨 (왼쪽에 표시)
- 약간 확대 효과

**완료된 태스크:**
- 🟢 초록색 "OK" 배지

**지난 태스크 (미완료):**
- 🔴 빨간색 "NOK" 배지 (Overdue)

**대기 중 태스크:**
- 🟡 노란색 "Waiting" 배지

### 4. 수동 스크롤 버튼

화면 우측 하단에 떠있는 버튼:
- 📍 위치: 우측 하단 (Auto Refresh 표시 위)
- 🔵 보라색 원형 버튼
- ⬇️ 아래 화살표 아이콘 (통통 튀는 애니메이션)
- 클릭 시: 즉시 다음 태스크로 이동

## 🎬 동작 방식

### 페이지 로드 시:
```
1. Site 선택 (예: Nerrilda)
2. Date 선택 (예: Today)
   ↓
3. 태스크 리스트 로드
   ↓
4. 다음 태스크 찾기 (현재 시간 이후 첫 미완료)
   ↓
5. 자동 스크롤 (페이지 중앙에 위치)
   ↓
6. 1초간 약간 확대 효과 (시각적 피드백)
```

### Auto Refresh 시 (5분마다):
```
1. 백그라운드에서 데이터 새로고침
   ↓
2. 태스크 리스트 업데이트
   ↓
3. 다시 다음 태스크로 자동 스크롤
```

### 수동 버튼 클릭 시:
```
1. 우측 하단 버튼 클릭
   ↓
2. 현재 시간 기준 다음 태스크 찾기
   ↓
3. 즉시 스크롤
```

## 📋 조건

### 자동 스크롤이 동작하는 경우:
✅ **오늘 날짜**를 선택한 경우에만
✅ 현재 시간 이후의 미완료 태스크가 있는 경우

### 자동 스크롤이 동작하지 않는 경우:
❌ 어제, 내일 등 **오늘이 아닌** 날짜 선택 시
❌ 모든 태스크가 완료된 경우
❌ 모든 태스크가 과거인 경우

## 🎨 UI 개선사항

### Before (기존):
```
┌─────────────────────────┐
│ Header                  │
├─────────────────────────┤
│ 08:00  Task 1  ✓ OK    │ ← 과거 (스크롤 필요)
│ 09:00  Task 2  ✓ OK    │
│ 10:00  Task 3  ✓ OK    │
│ 11:00  Task 4  ✓ OK    │
│ 12:00  Task 5  ✓ OK    │
│ 13:00  Task 6  ✓ OK    │
│ 14:30  Task 7  Waiting │ ← 다음 태스크 (안 보임!)
│ 15:00  Task 8  Waiting │
│ 16:00  Task 9  Waiting │
└─────────────────────────┘
```

### After (개선):
```
┌─────────────────────────┐
│ Header                  │
├─────────────────────────┤
│ 11:00  Task 4  ✓ OK    │ ← 자동 스크롤!
│ 12:00  Task 5  ✓ OK    │
│ 13:00  Task 6  ✓ OK    │
│ ▶NEXT 14:30  Task 7 🔴 │ ← 중앙에 위치!
│ 15:00  Task 8  Waiting │
│ 16:00  Task 9  Waiting │
│ 17:00  Task 10 Waiting │
└─────────────────────────┘
         [⬇️]  ← 수동 버튼
```

## 💻 코드 변경사항

### 1. 자동 스크롤 함수 추가

```javascript
function scrollToNextTask() {
    // 현재 시간 이후 첫 번째 미완료 태스크 찾기
    // 페이지 중앙에 부드럽게 스크롤
    // 1초간 강조 효과
}
```

### 2. CSS 개선

- `▶ NEXT` 라벨 추가 (빨간색 배지)
- 깜빡이는 애니메이션 강화
- 우측 하단 스크롤 버튼 (보라색 원형)

### 3. 호출 위치

- `renderScheduleFromBatchAPI()` 끝 부분
- `renderScheduleFromCache()` → `renderScheduleFromBatchAPI()` → 자동 호출
- Auto Refresh 시 자동 호출

## 🎮 사용 방법

### 기본 사용:
1. Site 선택
2. Date를 "Today" 선택
3. **자동으로 다음 태스크로 스크롤됨!**

### 수동 스크롤:
1. 스크롤하여 다른 태스크 확인
2. 다시 다음 태스크로 돌아가고 싶으면:
   - 우측 하단 **⬇️ 버튼 클릭**

### 날짜 변경:
- **어제/내일** 선택 시: 자동 스크롤 안 함 (맨 위부터 표시)
- **오늘** 선택 시: 자동으로 다음 태스크로 스크롤

## 📱 모바일 최적화

### 터치 제스처:
- 위/아래 스와이프: 스크롤
- 행 터치: Incident 상세 보기
- 우측 하단 버튼: 다음 태스크로 점프

### 성능:
- 300ms 지연으로 렌더링 완료 보장
- 부드러운 스크롤 애니메이션
- 가벼운 강조 효과 (1초)

## 🔧 커스터마이징

### 스크롤 위치 변경:

```javascript
// 중앙 (기본)
block: 'center'

// 상단
block: 'start'

// 하단
block: 'end'
```

### 감지 범위 변경:

```javascript
// 현재: 1시간 이내 (line 1027)
const isCurrent = !isPast && timeDiff >= 0 && timeDiff <= 60

// 예: 30분 이내로 변경
const isCurrent = !isPast && timeDiff >= 0 && timeDiff <= 30
```

### Auto Refresh 주기:

```javascript
// 현재: 5분 (line 1512)
5 * 60 * 1000

// 예: 3분으로 변경
3 * 60 * 1000
```

## 🧪 테스트 방법

### 1. 기본 시나리오:
```
1. 모바일 대시보드 접속: http://202.90.243.226/mobile_dashboard
2. Site 선택: Nerrilda
3. Date: Today 선택
4. 결과: 자동으로 다음 태스크가 중앙에 표시됨
```

### 2. 수동 버튼 테스트:
```
1. 위로 스크롤하여 과거 태스크 확인
2. 우측 하단 ⬇️ 버튼 클릭
3. 결과: 즉시 다음 태스크로 이동
```

### 3. 날짜 변경 테스트:
```
1. Date: Yesterday 선택
2. 결과: 맨 위부터 표시 (자동 스크롤 안 함)
3. Date: Today 선택
4. 결과: 다시 다음 태스크로 스크롤
```

### 4. Auto Refresh 테스트:
```
1. Today 날짜로 태스크 로드
2. 5분 대기
3. 결과: "Updating..." 표시 후 다시 다음 태스크로 스크롤
```

## 📊 예상 효과

### Before:
- ❌ 매번 수동으로 스크롤 필요
- ❌ 다음 태스크 찾기 어려움
- ❌ 시간 낭비

### After:
- ✅ 즉시 다음 태스크 확인
- ✅ 스크롤 불필요
- ✅ 빠른 업무 진행
- ✅ 모바일 UX 향상

## ⚠️ 주의사항

### 1. 오늘 날짜만 자동 스크롤
- 의도적인 디자인
- 과거/미래 날짜는 전체 보기가 유용

### 2. 다음 태스크가 없으면?
- 스크롤하지 않음
- 콘솔에 메시지 표시: "다음 태스크 없음"

### 3. 모바일 성능
- 부드러운 애니메이션 사용
- 배터리 소모 최소화

## 🔄 업데이트 내역

**날짜**: 2025-10-17

**변경사항**:
1. `scrollToNextTask()` 함수 추가
2. "▶ NEXT" 라벨 추가 (CSS)
3. 우측 하단 스크롤 버튼 추가
4. 자동 스크롤 로직 통합

**영향받는 파일**:
- `templates/mobile_task_dashboard.html`

**테스트 상태**:
- ⏳ 상용 서버 배포 대기
- ✅ 로컬 테스트 권장

## 📞 피드백

기능 사용 후 피드백 환영:
- Auto Refresh 시 스크롤이 방해되나요?
- 스크롤 위치가 적절한가요?
- 다른 개선사항이 있나요?

---

**URL**: http://202.90.243.226/mobile_dashboard  
**대상 사용자**: Registered Nurses, Carers  
**목적**: 업무 효율성 향상, 모바일 UX 개선

